/**
 * Sets the API base URL this feedback form talks to. Left EMPTY for local
 * Docker use -- see employee-dashboard/config.js for the full explanation.
 * Only set a real URL here if deploying standalone on a different origin
 * than the backend (e.g. Vercel without the nginx proxy).
 */
window.__AIRCARE_API_BASE__ = "";
