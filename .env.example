# Base URL of the deployed backend API (Phase 14 ALB domain, or localhost for dev).
# Baked in at build time since this is a static export -- rebuild when this changes.
NEXT_PUBLIC_API_URL=http://localhost:8000
